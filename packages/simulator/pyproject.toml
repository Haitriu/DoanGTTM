[project]
name = "voltrail-simulator"
version = "0.1.0"
description = "Simulator for Voltrail"
requires-python = ">=3.12"
dependencies = [
    "voltrail-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
voltrail-core = { workspace = true }
